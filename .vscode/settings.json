{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "pydb",
            "database": "${workspaceFolder:pydb}/pydb.db"
        }
    ]
}